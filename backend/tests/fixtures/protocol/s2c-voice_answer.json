{
  "name": "voice_answer",
  "direction": "server->client",
  "spec": "v1.1 6.10",
  "required": [
    "v",
    "type",
    "sdp",
    "mode"
  ],
  "message": {
    "v": 1,
    "type": "voice_answer",
    "sdp": "",
    "mode": "transcript"
  },
  "note": "The mode the server actually accepted, which need not be the one offered."
}
