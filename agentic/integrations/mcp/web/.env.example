# mcp-web configuration
# ─────────────────────────────────────────────────────────────
# Safety defaults (mandatory)
WRITE_ENABLED=false
DRY_RUN=true

# Server
PORT=9112
HOST=0.0.0.0

# Fetch safety
REQUEST_TIMEOUT=15
MAX_DOWNLOAD_BYTES=5242880
# Deny internal IPs (SSRF protection) — enabled by default
DENY_INTERNAL_IPS=true
