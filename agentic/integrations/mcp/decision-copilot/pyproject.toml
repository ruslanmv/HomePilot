[project]
name = "homepilot-mcp-decision-copilot"
version = "0.1.0"
description = "HomePilot MCP server: Decision Copilot (reference implementation)"
requires-python = ">=3.10"
dependencies = [
  "fastapi>=0.110",
  "uvicorn[standard]>=0.27",
  "pydantic>=2.0",
]
