# Local Development Environment Variables
# Copy this file to .env.local and customize as needed
# The backend will automatically load .env.local if it exists

# ==========================================
# LLM Provider (choose one)
# ==========================================

# Use Ollama (recommended for local development)
DEFAULT_PROVIDER=ollama
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=llama3:8b

# OR use vLLM / OpenAI-compatible API
# DEFAULT_PROVIDER=openai_compat
# LLM_BASE_URL=http://localhost:8001/v1
# LLM_MODEL=local-model

# ==========================================
# ComfyUI & Media Services (optional)
# ==========================================

# ComfyUI for image/video generation (optional)
COMFY_BASE_URL=http://localhost:8188

# Media service (optional)
MEDIA_BASE_URL=http://localhost:8002

# ==========================================
# Data Storage (local paths)
# ==========================================

# Data directory (auto-detected if not set)
# Will use ./backend/data for local development
# DATA_DIR=./backend/data

# Specific paths (optional overrides)
# SQLITE_PATH=./backend/data/homepilot.db
# UPLOAD_DIR=./backend/data/uploads
# OUTPUT_DIR=./backend/data/outputs

# ==========================================
# Security (optional)
# ==========================================

# API Key for authentication (leave empty for no auth in dev)
# API_KEY=your-secret-key-here

# ==========================================
# Generation Settings
# ==========================================

# Image model: sdxl, flux-schnell, flux-dev, pony-xl
IMAGE_MODEL=flux-schnell

# Video model: svd, wan-2.2, seedream
VIDEO_MODEL=svd

# Enable NSFW content generation (true/false)
NSFW_MODE=false

# ==========================================
# Performance & Limits
# ==========================================

# Maximum upload size in MB
MAX_UPLOAD_MB=25

# Tool execution timeout in seconds
TOOL_TIMEOUT_S=300

# ComfyUI polling settings
COMFY_POLL_INTERVAL_S=1.0
COMFY_POLL_MAX_S=240

# ==========================================
# CORS Origins (frontend URLs)
# ==========================================

# Comma-separated list of allowed origins
CORS_ORIGINS=http://localhost:3000,http://localhost:5173,http://127.0.0.1:3000

# Public base URL (for media URLs, optional)
# PUBLIC_BASE_URL=http://localhost:8000

# ==========================================
# MCP Context Forge (Agentic Features)
# ==========================================

# Enable MCP Gateway (set to false to disable agentic features)
MCP_ENABLED=true

# MCP Gateway URL
MCP_GATEWAY_URL=http://localhost:4444

# MCP Gateway auth token (auto-generated on first run, or set manually)
# MCPGATEWAY_BEARER_TOKEN=

# MCP Gateway Admin UI (must be true for /admin to work)
MCPGATEWAY_UI_ENABLED=true
MCPGATEWAY_ADMIN_API_ENABLED=true
# Disable login prompt so Admin UI opens without password (local dev)
AUTH_REQUIRED=false

# --- Agentic AI Layer ---
AGENTIC_ENABLED=true
CONTEXT_FORGE_URL=http://localhost:4444
CONTEXT_FORGE_ADMIN_URL=http://localhost:4444/admin
# CONTEXT_FORGE_TOKEN=
CONTEXT_FORGE_AUTH_USER=admin
CONTEXT_FORGE_AUTH_PASS=changeme
