AVATAR_SERVICE_PORT=8020
AVATAR_OUTPUT_DIR=../backend/data/uploads

# StyleGAN2 inference (optional — disabled by default)
# Set both to enable real face generation:
STYLEGAN_ENABLED=false
STYLEGAN_WEIGHTS_PATH=
STYLEGAN_DEVICE=auto
