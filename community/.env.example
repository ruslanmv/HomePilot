# Cloudflare Account ID — required for R2 uploads
# Find yours at: https://dash.cloudflare.com → any zone → Overview → Account ID
CLOUDFLARE_ACCOUNT_ID=your-account-id-here

# Optional: Cloudflare API Token (falls back to wrangler OAuth token)
# CLOUDFLARE_API_TOKEN=your-api-token-here
